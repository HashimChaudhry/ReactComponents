import react from "react"
import styles from "../../css/card.module.css

// A card component that has an image and title overlay
// Can be flipped to provide a description if needed
let ImageCard = ({ flip, text, image }) => {

	return (
		<div>
								
		</div>
	)
}

export default OverlayTextCard;
